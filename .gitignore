# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# pytest
.pytest_cache/

# Distribution / packaging
*.egg-info/
dist/
build/

# Index binaires (régénérables avec le pipeline)
data/indexes/faiss_index.bin
data/indexes/bm25_index.pkl
data/indexes/index_metadata.json

# OS
Thumbs.db
.DS_Store

# HuggingFace model cache — never inside this project, but just in case
*.bin
*.safetensors
*.gguf
# ML model artifacts only — NOT the backend source package backend/models/
/models/
!backend/models/
!backend/models/**

# Frontend build output and dependencies
frontend/node_modules/
frontend/dist/

# Python env
*.env
.env

# ── Runtime data — DO NOT COMMIT (private: accounts, chat history, uploads) ──
data/users.json
data/oncologia.db
data/history.json
data/uploads/

# ── Generated benchmark outputs (regenerable from the Benchmark page) ──
llm_benchmark_results.json
llm_benchmark_full.json

# ── Not for distribution ──
IMPLEMENTATION_GUIDE.html
Mini-projects-NLP.pdf
PROJECT_STATUS.md
PROJECT_STATUS_EN.md
